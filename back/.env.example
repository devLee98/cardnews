# OpenAI
OPENAI_API_KEY=
# 텍스트 모델 (기본 gpt-5)
OPENAI_TEXT_MODEL=gpt-5
# gpt-5 계열 추론 강도: minimal | low | medium | high
# 기본(medium)은 50초를 넘겨 nginx 타임아웃에 걸린다. 추론 모델이 아니면 빈 값으로 둘 것
OPENAI_REASONING_EFFORT=low
